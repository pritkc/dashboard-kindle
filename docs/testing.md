# Testing

Run:

```bash
pnpm test
pnpm lint
pnpm typecheck
pnpm build
```

The tests cover hashing, SSRF protection, quiet-hour scheduling, polling calculations, source collection, render artifacts, ETag behavior, simulator-style image validation, and backup/restore.
